export interface Unit {
  id: number;
  name: string;
  abbreviation: string;
  description: string;
}

export interface UnitPayload {
  name: string;
  abbreviation: string;
  description: string;
}
