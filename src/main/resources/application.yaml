spring:
  config:
    import: application.properties

  application:
    name: recipebook

  jpa:
    hibernate:
      ddl-auto: validate

  datasource:
    url: jdbc:postgresql://localhost:5432/recipebook
    username: postgres
    password: ${DB_PASSWORD}

server:
  port: 8080
